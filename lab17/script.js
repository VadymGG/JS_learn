const strings = ["JavaScript", "Python", "C#", "C++"];

const lengths = strings.map((str) => {
    return str.length;
});

console.log("Вихідний масив рядків:", strings);
console.log("Масив довжин рядків:", lengths);